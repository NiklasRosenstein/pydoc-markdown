release_date: null
changes:
- type: feature
  component: mkdocs
  description: add `server-port` option, treat `MKDOCS_PORT` environment variable
    if `server-port` option is not set
  fixes:
  - '#148'
