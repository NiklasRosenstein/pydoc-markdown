changes:
- type: feature
  component: MarkdownRenderer
  description: add `render_typehint_in_data_header` option to `MarkdownRenderer`
  fixes:
  - '#229'
- type: fix
  component: MarkdownRenderer
  description: no longer escape markdown in headers when `html_headers` is enabled
    (used to result in additional backslashes when interpreted by most Markdown to
    HTML renderers)
